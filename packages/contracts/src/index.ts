/**
 * Contratos compartilhados entre os servicos.
 *
 * O pacote existe desde o esqueleto para que publicador e consumidor dependam do mesmo
 * ponto de verdade desde o primeiro evento — os schemas entram aqui na fase do fluxo
 * assincrono.
 */

export const CONTRACTS_VERSION = '0.0.0';
